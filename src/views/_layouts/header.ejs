<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title><%= title %></title>
        <link href="https://fonts.googleapis.com/css2?family=Exo:ital,wght@0,700;1,500&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@600&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/fancybox/3.1.20/jquery.fancybox.min.css" />
        <link rel="stylesheet" href="/css/style.css" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
        <script type="module" src="https://cdn.jsdelivr.net/npm/@ionic/core/dist/ionic/ionic.esm.js"></script>
        <script nomodule src="https://cdn.jsdelivr.net/npm/@ionic/core/dist/ionic/ionic.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    </head>

    <body>
        <div class="ajaxbg">
            <div class="ajaxinner">
            </div>
        </div>
        <nav class="navbar navbar-inverse myNav">
            <div class="container ">
                <div class="navbar-header">

                    
                <div class="elements">
                  <img src="../../images/logo.png"><p class="title-name">FIE<span>SBS</span></p>
                  <div class="nav-buttons">
                  
                    <button onclick="window.location.href='/cart/checkout'"><span><ion-icon name="cart"></ion-icon> سلة </span> التسوق</button>
                    <button onclick="window.location.href='/'"><span><ion-icon name="home"></ion-icon> الصفحة</span> الرئيسية</button>
                    <% if (user) { %>
                        <button class="logout" onclick="window.location.href='/users/logout'"><%= user.username %> ( تسجيل خروج )</button>
                        <% } else {%>
                        <button class="login" onclick="window.location.href='/users/login'"><span><ion-icon name="person"></ion-icon> تسجيل</span> الدخول</button>
                        <% } %>

                    <% if (user && user.admin == 1) { %>
                        <button onclick="window.location.href='/admin/categories'" class="admin">مركز التحكم</button>
                        <% }%>

                  </div>
                </div>  
                    
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <% pages.forEach(function(page){ %>
                        <% if (page.slug == "home") { %>
                        <li><a href="/"><%= page.title %></a></li>
                        <% } else {%>
                        <li><a href="/<%= page.slug %>"><%= page.title %></a></li>
                        <% } %>
                        <% }); %>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                        <% if (user) { %>
                        <li><button class="logout" onclick="window.location.href='/users/logout'"></button></li>
                        <% } else {%>
                        <li><button class="login" onclick="window.location.href='/users/login'"></li>
                        <% } %>
                        <% if (user && user.admin == 1) { %>
                        <li><button onclick="window.location.href='/admin/categories'" class="admin"></button></li>
                        <% }%>
                        
                    </ul>
                </div><!--/.nav-collapse -->
            </div>
        </nav>

    <center>
        <div class="cat">
          
          <button class="left-right" onclick="change()"><ion-icon name="play-back"></ion-icon></button>
            
            <div id="first">
              <button>جميع المنتجات</button>
              <button>سيارات</button>
              <button>الكترونيات</button>
              <button>ميك اب</button>
            </div>

            <div id="second" style="display: none;">
              <button>اثاث</button>
              <button>ملابس</button>
              <button>اكسسوارات اجهزة</button>
              <button>منتجات امريكية بالجملة</button>
            </div>

          <button class="left-right" onclick="change()"><ion-icon name="play-forward"></ion-icon></button>
        </div>
    </center>

    <script>
      function change(){
    var first = document.getElementById("first");
    var second = document.getElementById("second");

    if(first.style.display == "none"){
        first.style.display = "inline-flex";
        second.style.display = "none";
    } else if(second.style.display == "none"){
        second.style.display = "inline-flex";
        first.style.display = "none";
    }
}
     </script>